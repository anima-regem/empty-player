/// High-level v2 app shell composition point.
///
/// This file intentionally stays lightweight while the new modules are
/// incrementally wired into production pages.
class AppShellV2 {
  const AppShellV2();
}
